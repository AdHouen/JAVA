public class Item5 {
    

    public static void main(String[] args) {
        double i = 4;
        double c = Math.PI/2;
        System.out.println(Math.sqrt(i));
        System.out.println(Math.cos(c));
        // System.out.format("%.10f", + (Math.cos(c))); //<=== avoir le cosinus avec par exmple 10décimales
        
    }

    // Utiliser la classe java.lang.Math pour afficher des calculs scientifiques 
    // Lecon retenu : 
    //     - Chaque methode est indépendante
    //     - pas besoin de faire la methode addition
    //     -mettre des parentheses pour mettre les priorités sur les calculs
    //     - un cacul (add, sous, etc) est toujours en int si l'on veut changer il faut le forcer ((byte) ou (float) etc)
    //     - System.out.format("%.1f" pour choisir le nombre de décimale
    
    
}
